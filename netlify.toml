[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

# SPA fallback. Functions with path /api/* take precedence.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
